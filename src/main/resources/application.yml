logging:
  file:
    path: .
    name: drones.log
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/drones
    username: ehizman
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create